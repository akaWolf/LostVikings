/* Lost Vikings embedding of libmt32emu (Munt) 2.8.3 — the CMake-generated
 * config.h written by hand: a static, C++-API-only build (no shared library,
 * no C API, no runtime version check). Same license as the library
 * (LGPL-2.1-or-later, COPYING.LESSER.txt). */
#ifndef MT32EMU_CONFIG_H
#define MT32EMU_CONFIG_H
#define MT32EMU_VERSION      "2.8.3"
#define MT32EMU_VERSION_MAJOR 2
#define MT32EMU_VERSION_MINOR 8
#define MT32EMU_VERSION_PATCH 3
#define MT32EMU_EXPORTS_TYPE 0
#endif

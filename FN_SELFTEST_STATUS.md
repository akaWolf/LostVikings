# FN-SELFTEST — статус покрытия процедур

Живой трекер synthetic-diff тестов (дизайн: SYNTHETIC_DIFF_ANALYSIS.md).
Запуск: `FNSELFTEST=<имя|all> ./vikings_headless` (HEADLESS-билд; игра не
стартует, SDL/потоки не поднимаются; секунды на всё покрытие).
Оракул = изолированный вызов оригинала (m2c) через штатный `m2c::CALL_`;
эталоны нигде не пишутся руками. Каждый НОВЫЙ тип сравнения обязан быть
доказан порчей (интенсиональный баг → тест падает → откат).

## Протестировано

| # | Функция | Роль | Контракт (вход → выход) | Кейсы | Результат | Порча-proof | Коммит |
|---|---------|------|--------------------------|-------|-----------|-------------|--------|
| 1 | `sub_15972` | collision Y-snap (tile boundary) | ax, di; DS[di+0x150D/0x14E5/0x1765] → те же 3 поля + DS[di+0x19E5]=0 | 282 432 = grid 288 (ветки×границы) + exhaustive **все 65536 ax** ×4 Y-набора + fuzz 20 000 | **PASS, 0 диффов** | ✔ двойная: (а) snap-ветка ax==0 (`-1`→`-2`) — 3 адреса, точный репро; (б) ветка **ax>0, которую replay не исполнял ни разу** (в 1000 кадрах attract только ax==0) — поймана | 731d209 |
| 2 | `sub_161a1` | collision bbox check (di=self, si=target) | di, si; 10 DS-полей (X: di/si+0x1535/0x155D; Y: di+0x150D/0x1765/0x13CD, si+0x14E5/0x1765/0x13CD) → CF (STC=коллизия); скретч ds:0x32/34/36 пишется только после X-reject'ов (канарейки 0xBBBB проверяют path-exact) | 161 272 = grid 200 (X-конфиги с equality-границами знаковых сравнений × 10 Y-наборов × 4 пары di/si) + exhaustive [si+0x14E5] и [di+0x150D] по 65536 + fuzz 30 000 | **PASS, 0 диффов** (включая хвост, ранее не сверявшийся вручную) | ✔ CF-канал: инверсия reject#1 → `cf orig=0 v2=1` ровно на reject#1-кейсах | 48dff3b |
| 3 | `sub_15da8` | Y-snap после объектной коллизии (di=self, si=partner) | ax (0=вверх/≠0=вниз), di, si; ax==0: delta=[di+0x150D]−[si+0x14E5]+1, ax≠0: delta=[si+0x150D]−[di+0x14E5]+1 → [di+0x1765/0x14E5/0x150D] ∓ delta, [di+0x19E5]=0 (канарейка 0xAAAA) | 151 240 = grid 168 (6 ax × 7 полевых наборов: delta 0/1/отрицательная-wrap, INC-wrap 0xFFFF, знаковые границы × 4 пары di/si) + exhaustive обоих delta-операндов ([di+0x150D], [si+0x150D]) по 65536 + fuzz 20 000 | **PASS, 0 диффов** | тип сравнения не новый (DS-дифф+канарейка — доказан на #1) | (этот) |
| 4 | `sub_15d6b` | X-snap близнец (поля 0x173D/0x1535/0x155D, акк. 0x19BD) | симметрично #3 по X-осям | 151 240 (та же схема, свой fuzz-seed) | **PASS, 0 диффов** | — (аналогично #3) | f369abb |
| 5 | `sub_13d68` | скан пула суб-спрайтов на непрерывный блок из [si+0x1AD5] свободных слотов | si, ds:0x374 (пул: ≥2→[0,0x30), 1→[0x30,0x50), 0→[0x48,0x100)), occupancy [d+0x44D]/[d+0x114D] → CF (STC=нет места); ds:0x32=лимит, ds:0x3A/0x38=кандидат start/end (пишутся на КАЖДОМ кандидате; канарейки 0xBBBB) | 40 088 = grid 88 (4 режима × дыры count−1/count, блок точно у JZ-границы лимита, OR-семантика 0x114D, count 0/wild) + fuzz 40 000 (случайные occupancy-карты) | **PASS, 0 диффов** | тип DS+CF известен | (этот) |
| 6 | `sub_13dd6` | init выделенных слотов [si+0x1A85..0x1AAD): 5 полей на слот | si; flags [si+0x1585], src [si+0x1855], ds:0x2E73 — оба ПЕРЕЧИТЫВАЮТСЯ каждую итерацию (orig); do-while (пустой/инвертированный диапазон пишет 1 слот) | 30 042 = grid 42 (пустой/инверт диапазоны, **alias-кейсы: диапазон накрывает ds:0x2E73 и si+0x1855 — цикл перезаписывает свои источники**, знаковые границы) + fuzz 30 000 | **PASS, 0 диффов** | — | (этот) |
| 7 | `sub_13e15` | тип/размер слотов (8×8/16×16) по flags bit0: [d+0xC4D]=8/0x20, OR [d+0x44D] 1/2 | si; do-while по [si+0x1A85..0x1AAD); прединициализированные 0x44D-паттерны проверяют OR поверх произвольных бит | 30 042 (та же схема) | **PASS, 0 диффов** | — | (этот) |

**Итого: 7 процедур, 846 356 кейсов, 0 расхождений.**

Заметка (#176): sub_13d68/13dd6/13e15 существовали в v2 как ДВА разошедшихся
инлайна (в v2_sub_13809 и в v2_vm_op_14); оба отличались от оригинала в углах
(pre-тест циклов вместо do-while → пустой диапазон давал 0 записей вместо 1;
hoisting чтений ds:0x2E73/[si+0x1855], которые orig перечитывает в цикле).
Извлечены в общие функции, написанные по asm; оба сайта переведены на вызовы;
углы доказаны alias/empty-кейсами селфтеста; full replay-регрессия 3/3 PASS
(в живых прогонах углы недостижимы — потому дрейф и жил незамеченным).

Валидация самой изоляции: кросс-чек оракула на живом реплее
(`FNTEST=sub_15972 FNTEST_XCHECK=1`, attract 1000 кадров) — изолированный
вызов воспроизводит живой выход байт-в-байт (ok=2, diverge=0).

## Очередь (класс A — DS-чистые, из коллизионно-скроллового кластера)

- sub_13d68, sub_13dd6, sub_13e15, sub_13c0c (collision/snap семейство — проверить сегментные зависимости tilemap)
- sub_1064b (camera clamp), sub_10704 / sub_10753 (scroll clamp)
- sub_101be (palette DEC pass), sub_105cb (exit-check: + CF/регистры)
- sub_1584e-цепочка — после аудита сегментных зависимостей (tilemap через
  сегменты = класс B-механика: тест-образы сегментов)

Дальше: класс B (VM-опкоды на синтетическом байткоде, es = тест-буфер;
цель — selftest на каждый опкод, сверка перечня с B5-coverage), класс C
(рендер, буферный дифф orig drawBuffer ↔ v2_render_buf), класс D
(декомпрессия, входы из DATA.DAT как файла). Класс E (фазовая
синхронизация, потоки, звук) — не для fn-тестов, остаётся за headless
replay (scenarios.sh).

## Как добавить функцию (конвейер)

1. Прочитать orig asm построчно → контракт: читаемые/писаемые DS-поля,
   регистры-входы, выходы (CF/ax), ветки и их границы.
2. `v2_fntest_orig_fnptr` (seg000): +1 строка `case N: return (void*)&sub_XXXXX;`.
3. Экспорт-обёртка v2-стороны в v2_vm.cpp (рядом с существующими).
4. v2_fn_test.cpp: enum FtId + g_name + кейс-раннер (поля контракта,
   канарейки на условно-писаемые адреса) + группы генераторов
   (grid по веткам/границам, exhaustive ключевых 16-бит операндов, fuzz
   с фиксированным seed) + строка в env-parse.
5. Прогон → 0 диффов; порча нового типа сравнения (если появился) →
   ловится → откат; коммит + строка в этой таблице.
